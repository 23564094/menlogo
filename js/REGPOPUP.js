$(window).ready(() => {
	$('#myModal').modal('show');
	// setTimeout(() => $('#myModal').modal('show'), 1000);
	// setTimeout(() => $('#myModal').modal('hide'), 4000);
})
const show = () => {
	$('#myModal').modal('show');
}
